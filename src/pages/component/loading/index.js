import  React from 'react';
import './loading.css'

const Loading = (props)=> (
    <div className="loader">
        <div className="loader-inner">

        </div>
    </div>
);

export default Loading;